apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "htmlapp.fullname" . }}-test-connection"
  labels:
    {{- include "htmlapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "htmlapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
